/** @type {import('next').NextConfig} */
const nextConfig = {
  // The game's index.html, game.js, levels/, songs/ etc. live in /public
  // and are served at the root automatically by Next.js.
};

module.exports = nextConfig;
